pipeline {
  agent any
  stages {
    stage('Initialisation') {
      steps {
        echo 'Initialisation of Flask Application'
      }
    }
    stage('Build') {
      steps {
        echo 'Print Build'
      }
    }
    stage('Deloy') {
      steps {
        echo 'Start deploy'
        echo 'End Deploy'
      }
    }
  }
}
